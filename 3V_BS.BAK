#include <stdio.h>
void main()
{
	int a,b,c;
	clrscr();
	printf("a value: ");
	scanf("%d",&a);
	printf("b value:");
	scanf("%d",&b);
	printf("c vlaue:");
	scanf("%d",&c);
	if(a==b&&b==c)
	{
		printf("numbers are the same.\n");
	}
	else if(a==b)
	{
		if(c>a)
		{
			printf("a,b are same, c is the largest.\n");
		}
		else
		{
			printf("a,b are same, a is the largest.\n");
		}
	}
	else if(b==c)
	{
		if(a>b)
		{
			printf("b and c are same, a is the largest.\n");
		}
		else
		{
			printf("b and c are same, b is the largest.\n");
		}
	}
	else if (a==c)
	{
		if(b>a)
		{
			printf("a,c are same, b is the largest.\n");
		}
		else
		{
			printf("a,c are same, a is the largest.\n");
		}
	}
	else
	{
		if (a>b&&a>c)
		{
			printf("a is the largest number.\n");
		}
		else if (b>a&&b>c)
		{
			printf("b is the largest number.\n");
		}
		else
		{
			printf("c is the largest number.\n");
		}
	}

	getch();
}
